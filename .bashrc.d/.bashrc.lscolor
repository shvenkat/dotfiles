# ls color
if type -ft dircolors >/dev/null; then
  test -r ~/.dircolors && eval "$(dircolors -b $HOME/.dir_colors)" || eval "$(dircolors -b)"
fi

# export GCC_COLORS='error=01;31:warning=01;35:note=01;36:caret=01;32:locus=01:quote=01'
